import { Request, Response } from "express";

export const authenticateByGoogle = (req: Request, res: Response) => {};

export const redirectToCallbackUri = (req: Request, res: Response) => {};

export const logout = (req: Request, res: Response) => {};
